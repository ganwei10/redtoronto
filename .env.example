# Anthropic Claude API Key（留空则使用规则生成的兜底结果）
ANTHROPIC_API_KEY=

# 可选：覆盖默认模型（默认 claude-3-5-sonnet-latest）
ANTHROPIC_MODEL=claude-3-5-sonnet-latest

# 生产数据库（升级到 Postgres 时填写，见 prisma/schema.prisma）
DATABASE_URL=

# NextAuth（生产多角色登录时填写）
NEXTAUTH_SECRET=
